[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# 🔴 IMPORTANT : redirect SPA pour React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
